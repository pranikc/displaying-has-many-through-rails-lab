<h1>All Patients</h1>

<ul>
    <% @patients.each do |patient| %>
        <li>
            <%= "Name: " + patient.name%> <br>
            <%= "Number of Appointments: " + patient.num_of_appts.to_s %>
        </li>
    <% end %>
</ul>
